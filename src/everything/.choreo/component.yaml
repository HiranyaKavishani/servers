# +required The configuration file schema version
schemaVersion: 1.2

endpoints:
  - name: mcp-test-everything
    displayName: MCP Test Everything
    service:
      basePath: /mcp_everything
      port: 3001
    type: REST
    networkVisibilities:
      - Public
      - Organization
    schemaFilePath: openapi.yaml
